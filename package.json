{
  "name": "klyvo-admin",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server/index.mjs",
    "dev": "node server/demo.mjs",
    "test": "node --test tests/*.test.mjs",
    "start:hosted": "node server/index.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "google-auth-library": "^10.0.0"
  }
}
